language: go

sudo: false

go:
  - 1.7
  - tip

install: make deps

script:
  - make build
  - ./build/chaosmonkey_linux_amd64 --version

branches:
  only:
    - master

matrix:
  allow_failures:
    - go: tip
